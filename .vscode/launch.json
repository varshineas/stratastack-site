{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Open Site in Browser (with Python Server)",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:8000",
            "webRoot": "${workspaceFolder}",
            "preLaunchTask": "Start Local Server"
        }
    ]
}